apiVersion: v1
kind: Service
metadata:
  name: {{ include "demands.fullname" . }}-keycloak
  labels:
    app: keycloak
    {{- include "demands.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.keycloak.port }}
      targetPort: {{ .Values.keycloak.port }}
      protocol: TCP
  selector:
    app: keycloak
