apiVersion: v1
kind: Service
metadata:
  name: {{ include "demands.fullname" . }}-postgres
  labels:
    app: postgres
    {{- include "demands.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.postgres.port }}
      targetPort: {{ .Values.postgres.port }}
      protocol: TCP
  selector:
    app: postgres
